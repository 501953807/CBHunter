# CBHunter 领域词汇表

## Materio 参考视觉系统

以 Materio Vue 电商后台为主要视觉参考的全局 UI 语言。它定义 CBHunter 的壳层、导航、卡片、表单、表格、弹层、图表和主题自定义的视觉基准；只约束表现和交互样式，不改变业务数据、接口、权限或路由语义。

## 全局壳层

包裹全部业务页面的固定结构，包括左侧系统菜单、顶部导航栏、内容工作区和右侧悬浮主题入口。全局壳层负责导航、主题、通知、同步和账号入口，不承载具体业务编辑逻辑。

## 共享组件底座

全站复用的基础 UI 组件集合，包括按钮、卡片、输入框、选择器、标签、表格、弹窗、通知、分页和空状态。页面级功能应优先复用共享组件底座，再补充模块专用样式。

## 页面级重构

在保留现有业务接口、字段、状态和路由的前提下，重新组织单个业务页面的布局、密度、视觉层级和操作入口。页面级重构不得把列表、详情、编辑、日志和后台映射全部平铺到同一个主操作区。

## 样式污染

旧版页面或组件残留的硬编码颜色、圆角、阴影、间距、字体或布局规则与当前全局视觉系统冲突，导致页面局部看起来不一致。样式污染应通过共享 token、共享组件或模块样式收口，不应暴力删除仍被业务依赖的代码。

## 业务逻辑保护线

UI 改造期间必须保留的系统边界：不改接口入参出参、不改数据模型含义、不改路由归属、不改权限判断、不伪造平台同步或发布结果、不把平台/店铺/商品多对多隔离关系合并污染。
